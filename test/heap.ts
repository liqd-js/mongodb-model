import * as assert from 'assert';

describe('- basic test', () =>
{
	it('should print ok', () =>
	{
		assert.ok( 'ok' === 'ok' );
	});
});

describe('- custom comparator', () =>
{
	it('should create heap and pop items', () =>
	{
		assert.ok( 'ok' === 'ok' );
	});
});